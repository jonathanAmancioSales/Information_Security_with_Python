#sudo apt-get install git-all

git --version

#Configurando o Git:
git config --global user.name "Jonathan Sales"
git config --global user.email "jonathan.sales@fisica.ufc.br"

#exibir todos os itens de configuração:
git config --list

git init

#Criar arquivos
#echo "..." > file.txt

git add *
